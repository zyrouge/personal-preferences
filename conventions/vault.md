# vault

```
Vault
├── ZDev
│   └── `<git repositories...>`
├── Tools
│   ├── paths.sh
│   └── `<tools...>`
└── Secrets
    └── `<secrets...>`
```

## `~/Vault/Tools/paths.sh`

```bash
#!/bin/sh
export PATH="$PATH:$HOME/Vault/Tools/opencode"
```
